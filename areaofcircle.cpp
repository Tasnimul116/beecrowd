#include <bits/stdc++.h>

using namespace std;

const double pi = 3.14159;

int main() {
    double R, A;

    cin >> R;

    A = pi * (R * R);

    cout << fixed << setprecision(4) << "A=" << A << endl;

    return 0;
}
